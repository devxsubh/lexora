/**
 * API v2 — mount point for future breaking changes. Currently mirrors v1 behavior.
 * Prefer `/api/v2` for new clients; `/api/v1` remains stable (frozen contract).
 */
export { default } from '../v1';
